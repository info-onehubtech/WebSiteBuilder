<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name ? name : 'Test Site' %> - Agency</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
  <h1><%= name ? name : 'Welcome to the Test Site' %></h1>
  <p><%= description ? description : 'This is a sample description for your test template.' %></p>
  </header>
  <main>
    <section>
      <h2>About</h2>
  <p><%= about ? about : 'About section content goes here.' %></p>
    </section>
    <section>
      <h2>Services</h2>
      <ul>
        <% if (services && services.length) { %>
          <% services.forEach(function(service) { %>
            <li><%= service %></li>
          <% }); %>
        <% } else { %>
          <li>Service 1</li>
          <li>Service 2</li>
        <% } %>
      </ul>
    </section>
    <section>
      <h2>Contact</h2>
  <p>Phone: <%= contact && contact.phone ? contact.phone : 'N/A' %></p>
  <p>Email: <%= contact && contact.email ? contact.email : 'N/A' %></p>
  <p>Address: <%= address ? address : 'N/A' %></p>
    </section>
  </main>
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Test Site. All rights reserved.</p>
  </footer>
</body>
</html>
